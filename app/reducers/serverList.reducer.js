const defaultState = {
    servers: []
};

const serverListReducer = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default serverListReducer;
